apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    //buildToolsVersion '26.0.2'

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
    }

    sourceSets {
        main {
            assets.srcDirs = ['assets']
        }
    }
}